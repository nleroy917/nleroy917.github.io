//!
//! This crate provides the core functionality for the static site generator (SSG). It includes modules for configuration, content management, and rendering.
//! The main entry point is in `src/main.rs`, which handles command-line arguments and orchestrates the build and development processes.
pub mod render;